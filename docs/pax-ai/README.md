# pax-ai docs

`pax-ai` isn't a repo yet — it's a planned sibling crate to `pax-core` (see
[`../README.md`](../README.md) for the architecture reasoning). There's no
spec/status/build-log here yet for the same reason: those describe an
existing repo's target vs. actual state, and `pax-ai` doesn't have either
until it exists.

- [`ideas.md`](ideas.md) — feature idea backlog (9 ideas): everything that
  needs an LLM call, embeddings, or a derived cache.
