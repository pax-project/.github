# lazy-pax docs

The only thing that doesn't fit a GitHub Issue/Discussion/Project — fixed
architectural boundaries, not backlog items or progress to track.

- [`dod.md`](dod.md) — the original Definition of Done for the `lazypax`
  MVP. Historical, unchanged for now.

The idea backlog, implementation-status checklist, and build history that
used to live alongside it moved to `lazy-pax`'s Issues and the shared
org-wide Project.
